import express, { Application } from 'express';

const app: Application = express();
app.use(express.json());

// Detalhes/funcionalidades da aplicação:


// app.use(handleError);

export default app;
